<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="usuario.app.agenda" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".AgendaActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ConsultarActivity"
            android:label="@string/title_activity_consultar" >
        </activity>
        <activity
            android:name=".EditarActivity"
            android:label="@string/title_activity_edit" >
        </activity>
        <activity
            android:name=".incluirActivity"
            android:label="@string/title_activity_incluir" >
        </activity>
        <activity
            android:name=".ExcluirActivity"
            android:label="@string/title_activity_excluir" >
        </activity>
    </application>

</manifest>
